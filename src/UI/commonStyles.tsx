import { Box, styled } from "@mui/material";

export const OverflowHidden = styled(Box)`
  overflow: hidden;
  text-decoration: none;
  color: inherit;
` as typeof Box;

export const TextUnderline = styled(Box)`
  height: 0.3rem;
  width: 100%;
  border-radius: 0.5rem;
  transform-origin: left;
` as typeof Box;